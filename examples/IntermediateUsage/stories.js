import React from 'react'
import IntermediateUsage from './index'

export default {
  title: 'IntermediateUsage',
  component: IntermediateUsage,
}

const Template = (args) => <IntermediateUsage {...args} />

export const Default = Template.bind({})
